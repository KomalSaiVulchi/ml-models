# AirPulse Frontend

This directory contains the React + Vite dashboard for AirPulse. For project overview, setup, and backend details, see the root [README](../README.md).

## Run

```bash
npm install
npm run dev
```

The frontend expects the Flask backend to be running on port `5001`.
